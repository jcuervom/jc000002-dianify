#!/usr/bin/env bash
echo "-----> Post-compile: Playwright setup should be handled by buildpack"
echo "-----> Verifying browser installation..."

if [ -f "/app/.playwright-browsers-path" ]; then
    echo "-----> Playwright browsers path file found"
    cat /app/.playwright-browsers-path
fi

echo "-----> Post-compile complete"
